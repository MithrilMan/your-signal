# Store assets

The screenshots in `screenshots/` are 1280×800 Chrome Web Store assets generated from synthetic, offline content. They contain no account data or live X posts.

Regenerate them with:

```powershell
python scripts/capture_store_assets.py
```
